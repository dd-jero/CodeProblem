#include <iostream>
#include <cmath>
using namespace std;

long long n;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(0);
	cout.tie(0);

	cin >> n;

	cout << n*n*n << "\n" << 3;

	return 0;
}

